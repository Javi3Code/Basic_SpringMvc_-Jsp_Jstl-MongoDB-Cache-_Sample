const letterRegex = /^[a-zA-z\s]/;
// function acceptOnlyLetters(ev) {
//     var letter = String.fromCharCode(ev.keyCode)
//     if (letterRegex.test(letter)) {
//         return true;
//     }
    // ev.preventDefault();
    // return false;
// }

function limitAge(element) {
    if (Number.parseInt(element.value) > 99) {

    }
}
